doctype html
html
  head
    title Artifacts
  body
    h1 Artifacts Search Results
    p Display results for artifacts.
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.render('artifacts', { title: 'Search Results - Artifacts' });
});

module.exports = router;
